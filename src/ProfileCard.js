function ProfileCard({title,handle}){
    return (
        <div>
            <h1>Title is {title}</h1>
            <h1>Handle is {handle}</h1>
        </div>
        
    );
}

export default ProfileCard;